# -*- coding: utf-8 -*-
"""章节表：底部进度条、顶部 HUD、章首大字卡三件共用这一张表。

CH  = [(章名, [段id, ...]), ...]   章名 ≤6 字，按片中顺序排，覆盖全部段
SEC = {段id: 小节名}               每段一个，HUD 上显示成「章名 › 小节名」

⛔⛔ 改稿重排段落 / 拆段之后**这张表必须跟着改**。少写一段会直接 KeyError，
   而且是卡在 final_pass 的最后一步——前面几十分钟的活白干。subs.py 里有一道 assert 提前报。
⛔ 章名是给观众定位用的，不是标题党：用「一口井」「叠成一栋楼」这种画面词，
   别用「第三部分：技术原理」。
"""

CH = [
    ("为什么涨", ["p01"]),
    ("拆开看", ["p02"]),
]

SEC = {
    "p01": "先看一个数字",
    "p02": "里面有什么",
}
