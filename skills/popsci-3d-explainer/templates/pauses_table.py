# -*- coding: utf-8 -*-
"""停顿落点表（片子内容，pauses.py 读它）。

    (段id, 落点串, "TURN" | "SWITCH")

落点串 = 该段显示文本里的**唯一**子串，停顿补在它**前面**。
    TURN   转折（「但」「错了」「真正的原因是」）    目标间隙 0.52 s
    SWITCH 换内容（「再往下是」「第二种说法」）      目标间隙 0.38 s

一段 3~8 处，落在每个语义转折的第一个字前面。别撒太密——TTS 自带的自然停顿已经够用，
这张表只管「机器读不出来的那几处语气转折」。

⛔⛔ 落点失效是**静默**的。句子被改掉、或者段被拆开之后，`plan()` 只打印一行
   「!! pXX 找不到落点」，日志一长根本看不见，于是那一整段一处停顿都没插过。
   **改完稿一定要 `python pauses.py plan`，逐行看有没有「找不到落点 / 落点不唯一」。**
⛔ 插完必须重跑 align——字幕和拍点都从 align 出，不重对齐会整段错位。
"""

PAUSES = [
    ("p01", "但问题来了", "TURN"),
    ("p01", "这期讲透三件事", "SWITCH"),

    ("p02", "再往下看", "SWITCH"),
]
